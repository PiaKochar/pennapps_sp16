var NeighborhoodParser = function ($neighborhood) {
	this.$neighborhood = $neighborhood;
}

NeighborhoodParser.prototype.get_data = function() {
	// do all of the work of getting the data and surfacing
	// it in the correct place - to start with maybe just
	// in the popup window, but also consider embedding
	// in the site
};